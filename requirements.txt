# MidiGPT — Python dependencies
#
# Install:
#   pip install -r requirements.txt
#
# IMPORTANT — torch installation:
#   The torch line below installs the CPU build by default.
#   For CUDA (NVIDIA GPU) builds, install torch SEPARATELY first:
#
#       # CUDA 12.1
#       pip install torch --index-url https://download.pytorch.org/whl/cu121
#       # CUDA 11.8
#       pip install torch --index-url https://download.pytorch.org/whl/cu118
#
#   ...then run:
#       pip install -r requirements.txt
#   (pip will see torch is already installed and skip it)

# --- Core ML ---
torch>=2.0,<3.0          # PyTorch 2.x for FlashAttention via scaled_dot_product_attention
numpy>=1.24,<3.0

# --- MIDI I/O ---
pretty_midi>=0.2.10      # MIDI parsing for tokenizer/encoder + augmentation
mido>=1.3.0              # Used by agents/reviewer.py for low-level MIDI inspection
