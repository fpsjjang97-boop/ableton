================================================================
  MidiGPT 9차 베타 리포트 — 테스터(유환) → 개발자(지수)
  날짜: 2026-04-21
  테스트 환경: Windows 10, RTX 4080, Python 3.11.9, PyTorch 2.5.1+cu121
================================================================


================================================================
[이번 테스트 진행 순서]
================================================================

1. git pull (3e48198 포함 — min_bars EOS 가드, 회귀 스크립트 개선) ✅
2. test_generate.py 생성 테스트 (pull 직후, SFT 재실행 전) ✅
3. midigpt_pipeline\sft 삭제 후 SFT 재실행 ✅
4. test_generate.py 생성 테스트 (SFT 재실행 후) ✅


================================================================
[SFT 학습 결과 — 정상 완료]
================================================================

  SFT pairs:   585개 (Skipped: 910, length=908)
  Epochs:      20
  Best loss:   0.2410
  NaN:         0
  Valid:       147/147
  소요시간:    5.5분
  LoRA 저장:   checkpoints\lora\lora_sft_best.bin ✅

  Loss가 Epoch 1(0.6843) → Epoch 20(0.2410)으로 안정적으로 수렴함.


================================================================
[핵심 문제 — min_bars EOS 가드 적용 후에도 결과 동일]
================================================================

■ 증상
  git pull → SFT 재실행 → test_generate.py 실행 모두 완료했으나
  생성 결과가 8차와 완전히 동일함.
  초반 몇 마디에만 노트가 뭉쳐있고 나머지 구간은 비어있음.

■ 의심 사항 1 — active_lora: None
  test_generate.py 실행 시 출력:

    {'loaded': True, 'device': 'cuda', 'active_lora': None,
     'vocab_size': 527, 'model_params': '48.1M',
     'gpu': 'NVIDIA GeForce RTX 4080', 'vram_gb': 17.2}

  active_lora 가 None 으로 표시됨.
  LoRA 가중치(lora_sft_best.bin)가 실제로 로드되고 있는지 확인 필요.
  LoRA가 적용 안 된 채로 base model만 생성에 사용되고 있을 가능성.

■ 의심 사항 2 — min_bars EOS 가드 미적용
  engine.py 에 min_bars=8 기본값이 추가됐지만
  test_generate.py 가 이 파라미터를 실제로 전달하고 있는지 불명확.
  test_generate.py → engine.py 호출 경로에서
  min_bars 가 실제로 작동하고 있는지 확인 요청.

■ 확인 요청
  (1) test_generate.py 가 LoRA 를 로드하는 경로 점검
      active_lora: None 이 정상인지, 아니면 로드 실패인지
  (2) test_generate.py → generate_to_midi 호출 시
      min_bars 파라미터가 전달되고 있는지 점검


================================================================
[SFT 페어 빌드 — length skip 여전히 다수]
================================================================

  Total SFT pairs:       585개
  Skipped at build time: 910개 (length=908, empty=2, tokenize=0)

  8차에서도 동일한 수치였음. window_bars / max_pair_tokens
  조정이 필요한지 재확인 부탁드림.


================================================================
[환경 정보]
================================================================

OS        : Windows 10 (19045.6691)
Python    : 3.11.9 (venv)
GPU       : NVIDIA GeForce RTX 4080 (17.2 GB)
PyTorch   : 2.5.1+cu121
CUDA      : 12.1
작업 경로  : C:\Users\USER\ableton

================================================================
  END OF REPORT
================================================================
