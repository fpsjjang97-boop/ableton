
# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# JUCE build
juce_app/build/
*.obj
*.pdb
*.ilk

# Build artifacts
dist/
build/
*.spec
*.exe

# Model checkpoints & training data (large files)
checkpoints/
midigpt_data/
midigpt/dpo_pairs/
midigpt_pipeline/augmented/
midigpt_pipeline/tokenized/
midi_data_combined/
raw_midi/
*.pt
*.onnx

# Test outputs (generation results, not source assets)
원신_output.mid
*_output.mid

# Generated MIDI outputs
output/

# Audio output (large WAV/MP3 files)
audio_to_midi_output/
*.wav
*.mp3
*.flac

# Temp
*.tmp
build_log.txt
fluidsynth/
C:/soundfonts/

# Internal logs & feedback (sensitive)
work_log.jsonl
피드백
db 파일 피드백/
260327_*.json
260329_*.json
260329 *.txt
DB구성*.txt
임시db.zip
Troubleshooter

# IDE
.vscode/
.idea/
*.swp

# Commercial software — NEVER commit. Protects repo from IP contamination.
# Reference materials (Cubase, other DAWs) must stay in separate directories
# outside this repo (e.g. D:/Cubase/).
*.7z
*.iso
*.dmg
*.msi
Cubase*
Steinberg*
cubase_*
steinberg_*
# Decompilation output
*.ghidra
*.gpr
ghidra_projects/

# JUCE framework — cloned locally, not part of this repo
juce_daw_clean/external/
juce_daw_clean/build/
juce_daw_clean/Builds/

# Quarantined legacy JUCE app (Ghidra-derived, do not touch)
juce_app_quarantine/

# Audit / temp outputs
audit_after_fix.csv
_tmp_plugin_*.mid
midigpt_pipeline/sft_audit*.json

# Third-party cloned repos (ADTOF 등 — download_checkpoints.py 가 재생성)
third_party/

# Training / server run logs and ephemeral state
pipeline_run.log
pipeline_pid.txt
server_run.log
server_generated*.mid
midigpt_pipeline/
reviews/
output/

# Claude Code local state (user-specific, do not share)
.claude/settings.local.json
.claude/worktrees/
